# Parent Equivalence and Omission Audit — R3

R3 does not claim semantic equivalence from topic names. It mechanically binds:

- the exact 25 V5 doctrine candidate IDs, bodies, and statement classes;
- the exact R5 + additive R6 active scar set;
- internal-governance rule IDs, bodies, and surface;
- project-obligation inactivity by default;
- execution/release operational statements;
- exact legacy topic identity and honest migration status;
- dormant substrate-profile activation rules;
- exact package membership and source hashes.

All 19 legacy topic identities now have a direct candidate destination. Replacement is still blocked by the required fresh real-project shadow session; coverage presence is not semantic proof.

Mechanical binding does not prove that the operational compression is the best wording, that every R6 semantic detail has been captured, or that a future project will make the same decision.

`SOURCE_CLASS_BOUND != SEMANTIC_FINALITY`
`SOURCE_BODY_BOUND != ALL_CONTEXT_RECOVERED`
