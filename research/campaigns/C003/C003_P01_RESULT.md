# C003 / P01 — host-subsidy recovery failure + first freestanding discriminator

**Disposition:** PASS COMPLETED / SCIENTIFIC SUCCESS CRITERION NOT MET / QUALIFIED NEGATIVE RESULT
**Scientific pass:** C003/P01 of 20
**Architecture promotion:** NONE
**P02 earned:** YES — by reconciled P01 result, not by pretending P01 scientific success

## Preregistered question

Which Python-host capabilities were silently carrying the C002 whole-P01 relation composition, and what is the smallest freestanding x86/QEMU executable slice that can falsify one high-value hidden dependency without importing Process/Scheduler/File/Manager/Service as primitives?

## Result in one sentence

The exact final C002 Python mechanism/fixture/launcher/evaluator source could not be recovered after bounded File Library, filesystem, archive-name, archive-content, and provenance searches, so the source-grounded host-subsidy success criterion was not met; however, the separately qualified freestanding discriminator showed that the P17/P18 current-completion distinction survives in a 512-byte fixed-state x86/QEMU embodiment without Python runtime services, while a one-shot negative control reproduces the completion-before-wait loss.

## Source-recovery outcome

**UNRECOVERED:** exact final C002 mechanism, fixture, launcher, and evaluator bytes.

Recovery surfaces exercised include:

- verified reincarnation payload history;
- OpenAI File Library exact P17/P18/P19 vocabulary and C002 mechanism/evaluator queries;
- local exact-name scans on `C:\Users\ancal`, `D:\`, and `E:\`;
- targeted old text/export surfaces;
- server-side research index;
- ZIP archive-content search;
- comprehensive archive logs covering:
  - C: 4,185 archives / 1,817,761 entries / 232 captured errors;
  - D: 153 archives / 43,003 entries / 4 captured errors;
  - E: 2,495 archives / 766,135 entries / 6 captured errors.

Filtered meaningful C002/P17/P18/P19 hits resolve to the reincarnation package, C001's preregistration of C002, or unrelated false positives. No hidden final C002 source bundle was recovered.

This is strong practical recovery evidence, not proof of universal nonexistence.

## Host-subsidy map result

Because no exact final C002 source was recovered, no suspect service is promoted to `relied on` merely from the suspect list.

The current map therefore records actual C002 reliance as `UNKNOWN` for:

- dynamic allocation;
- Python object identity;
- dict/list/set ordering and membership semantics;
- arbitrary-width integers;
- automatic lifetime/reference handling;
- exception control flow;
- strings/labels;
- host file I/O;
- collection mutation semantics;
- implicit memory safety;
- implicit atomicity;
- host scheduling/timing;
- interpreter stack/continuation;
- default initialization;
- serialization/conversion helpers.

The surviving C002/P20 source does explicitly identify containers, dynamic allocation, object identity, exceptions, collection behavior, and host execution semantics as **possible hidden burden**, not proven requirements.

## Freestanding discriminator outcome

Durable run: `20260829T205125Z_p01_reconstruction_01`

Mechanism / fixture / launcher / evaluator were separate and hashed. The fixture varied order only and did not supply wake/currentness behavior.

Observed guest consequence:

```text
R_WC=PASS
R_CW=PASS
B_CW=FAIL
DONE
```

Interpretation:

- repaired WAIT -> COMPLETE converged;
- repaired COMPLETE -> WAIT converged;
- deliberately broken one-shot COMPLETE -> WAIT remained broken;
- evaluator matched the exact expected matrix;
- QEMU completed with the expected deterministic exit;
- QEMU and evaluator stderr were empty.

Raw image:
- size: 512 bytes
- SHA-256: `962e7cba0ef2452b3d860f7ade7582aaee61f9f5430bf455864481f7c974cc3d`

Run receipt SHA-256:
- `036ccbcc3a758e74bb13922ffb305a24d1ce9b9aedbe5c721a0749bc1a1da48a`

## What P01 earned

Verified only for the reconstruction:

- the P17/P18 current-terminal-condition distinction does not require a Python runtime;
- it does not require dynamic allocation for the tested bounded case;
- it does not require Python object identity, dict/list/set containers, exceptions, or interpreter scheduling for the tested bounded case;
- explicit fixed-capacity current state is sufficient for the tested two-order discriminator;
- the one-shot form remains causally distinguishable.

These are **absence-of-necessity results for this slice**, not statements about what the lost C002 implementation actually used.

## Why P01 is not marked successful

The P01 preregistration required both:

1. a source-grounded host-subsidy map based on recovered C002 mechanism/evaluator source; and
2. a qualified freestanding consequence.

Only item 2 was earned. Therefore `SCIENTIFIC SUCCESS = false`.

C003's campaign-level preregistration explicitly permits useful failure. This pass is therefore closed as a qualified negative result rather than kept artificially open forever or relabeled green.

## P02 discriminator earned by P01

P01 removes the current-completion slice from the list of places where Python runtime machinery is obviously necessary. The next highest-value bounded host-subsidy pressure is the C002 survivor where **raw numeric policy position drifts under membership mutation**, while **identity-bound policy history plus coherent lifecycle repair** survived.

That survivor directly pressures several still-UNKNOWN host suspects at once:

- Python container ordering/membership semantics;
- dynamic allocation;
- Python object identity;
- collection mutation semantics;
- implicit atomicity.

Therefore P02 is earned as a fixed-capacity membership-mutation / identity-bound-history discriminator. P02 SHALL be preregistered separately before execution. P03-P20 remain unwritten.
