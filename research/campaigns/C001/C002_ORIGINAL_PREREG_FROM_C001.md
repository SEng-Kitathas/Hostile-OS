# C002 preregistration — Donor-neutral relation composition / whole-P01 closure

**Cadence:** exactly 20 passes, hard stop after C002/P20.

**Primary discriminator:** Can the C001 relation set compose into the complete P01 consequence workload without historical subsystem primitives?

**Forbidden shortcuts:**
- no `Process`, `Scheduler`, `File`, `Manager`, `Service` primitive by name;
- no ECS promotion merely because typed relations are convenient;
- no donor algorithm copied as architecture;
- no test harness that supplies missing cognition/control behavior;
- no successful prototype promoted to canonical OS architecture without hostile ablation and independent qualification.

**Required whole workload consequences:** boot/initialization boundary, finite activity, multiple progress-capable activities, block/wait, wake, child/parent return, persistent bytes across restart, bounded missing-operation failure, asynchronous/event consequence, idle/no-useful-work behavior.

**First pass C002/P01:** build the smallest explicit relation graph/state machine that can represent the already-qualified P01 events without executing policy. Attack it for ontology leakage and accidental historical nouns before adding an executor.
