from pathlib import Path
import json,sys
expected=['S1_8K_OK','TEST=C004_P04','B_BEFORE=W','B_BEFORE_VAL=7E','RES_GEN=01','RES_EPOCH=01','A_AFTER=W','A_AFTER_VAL=7E','B_OLD_AUTH=U','B_OLD_AUTH_VAL=00','B_RESOURCE_ONLY=W','B_RESOURCE_ONLY_VAL=7E','DONE']
trace=Path(sys.argv[1]).read_text(encoding='ascii',errors='replace').splitlines();checks={'trace_exact':trace==expected,'resource_currentness_unchanged':all(x in trace for x in ['RES_GEN=01','RES_EPOCH=01']),'a_survives_revoke':all(x in trace for x in ['A_AFTER=W','A_AFTER_VAL=7E']),'b_old_authority_revoked':all(x in trace for x in ['B_OLD_AUTH=U','B_OLD_AUTH_VAL=00']),'resource_only_bad_control_still_works':all(x in trace for x in ['B_RESOURCE_ONLY=W','B_RESOURCE_ONLY_VAL=7E'])};out={'format':'C004_P04_EVALUATION_V1','passed':all(checks.values()),'checks':checks,'trace':trace};Path(sys.argv[2]).write_text(json.dumps(out,indent=2)+'\n',encoding='utf-8',newline='\n');print(json.dumps(out,indent=2));raise SystemExit(0 if out['passed'] else 1)
